# Optional static frontend hosting. Configure frontend/runtime-config.js first.
# Recommended deployment: Node/Express serves frontend/dist and /api together.
[build]
  publish = "frontend/dist"
  command = "npm ci --prefix frontend && npm run build --prefix frontend"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
